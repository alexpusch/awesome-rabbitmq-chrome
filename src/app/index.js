import React from 'react';
import ReactDOM from 'react-dom';
import QueuesTable from './QueuesTable';

export default root => {
  ReactDOM.render(
    <div>
      <QueuesTable />
    </div>,
    root
  );
};
